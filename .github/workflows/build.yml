name: Build

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        include:
          - os: ubuntu-latest
            build-command: yarn build linux
            artifacts: |
              dist/*.deb
              dist/*.AppImage
              dist/*.rpm
              dist/*.snap
          - os: windows-latest
            build-command: yarn build windows
            artifacts: dist/*.exe
          - os: macos-latest
            build-command: yarn build macos
            artifacts: dist/*.dmg

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: yarn install

      - name: Build
        run: ${{ matrix.build-command }}

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          path: ${{ matrix.artifacts }}
          name: ${{ runner.os }}-artifacts
          if-no-files-found: error
