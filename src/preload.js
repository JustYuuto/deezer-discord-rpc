const alertText = 
`READ THE TEXT BELOW BEFORE CLOSING THIS WINDOW!!

A deezer login page will open. Login to your account, then it will redirecting to Deezer. You can close the window next after your login (it will run in background).

The web player will automatically update the current song, so don't worry the RPC it will update by itself.`;

alert(alertText);